import React from "react";
import Filter from "./Filter/Filter.js";
import "./HeroCatalog/heroCatalog.css"


const Catalog = (props) => {
    return (
        <div>
            <Filter/>
        </div>
    );
};

export default Catalog;